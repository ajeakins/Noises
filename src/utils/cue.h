#pragma once

#include <QString>

namespace noises
{
namespace utils
{
	void getNewCueNumber(
		const QString& previous_number,
		const QString& next_number,
		QString& new_number );

} /* namspace noises */
} /* namespace utils */
